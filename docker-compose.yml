version: "3.9"

services:
  web:
    build: .
    ports:
      - "8010:8000"
    environment:
      APP_SECRET: "change-me"
      DATABASE_URL: "sqlite:////data/app.db"
    volumes:
      - app_data:/data

volumes:
  app_data:
